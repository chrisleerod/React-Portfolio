import React from "react";

import {
    Spacer
} from './index.style';

const ProjParagraph = ({ spacer }) => {
    return (
        <Spacer spacer={spacer} />
    );
};

export default ProjParagraph;